# Monk OS DNS Configuration
#
# Monk OS uses the host system's DNS resolution via Bun's fetch API.
# This file exists for compatibility but is not actively used.
# Network requests are handled by the HAL (Hardware Abstraction Layer).

# Placeholder - actual resolution delegated to host
nameserver 127.0.0.1
