{
    "name": "httpd",
    "handler": "/usr/httpd/sbin/httpd.ts",
    "description": "HTTP server (kernel process)",
    "activate": {
        "type": "manual"
    },
    "defaults": {
        "port": 8080,
        "hostname": "localhost"
    }
}
