{
    "name": "@monk-api/os",
    "version": "0.10.0",
    "description": "A Plan 9/BeOS-inspired operating system where Bun is the hardware",
    "type": "module",
    "main": "src/index.ts",
    "workspaces": [
        "packages/*"
    ],
    "exports": {
        ".": "./src/index.ts",
        "./*": "./src/*"
    },
    "scripts": {
        "start": "bun scripts/start.ts",
        "start:sqlite": "bun scripts/start.ts --sqlite .data/monk.db",
        "shell": "bun scripts/shell.ts",
        "shell:sqlite": "bun scripts/shell.ts --sqlite .data/monk.db",
        "build": "scripts/build.sh",
        "build:warn": "scripts/build-warn.sh",
        "build:standalone": "scripts/build-standalone.sh",
        "test": "bun test --preload ./spec/bun-test-setup.ts spec",
        "test:coverage": "bun test --coverage --preload ./spec/bun-test-setup.ts spec",
        "perf": "bun test --preload ./spec/bun-test-setup.ts ./perf/**/*.perf.ts",
        "perf:coverage": "bun test --coverage --preload ./spec/bun-test-setup.ts ./perf/**/*.perf.ts",
        "perf:fixtures": "bun run ./perf/fixtures.ts",
        "typecheck": "bun x tsc --noEmit && bun x tsc --noEmit -p tsconfig.spec.json && bun x tsc --noEmit -p tsconfig.perf.json && bun x tsc --noEmit -p tsconfig.rom.json",
        "typecheck:src": "bun x tsc --noEmit",
        "typecheck:spec": "bun x tsc --noEmit -p tsconfig.spec.json",
        "typecheck:perf": "bun x tsc --noEmit -p tsconfig.perf.json",
        "typecheck:rom": "bun x tsc --noEmit -p tsconfig.rom.json",
        "lint": "eslint src rom spec",
        "lint:fix": "eslint src rom spec --fix",
        "audit": "bun scripts/audit.ts",
        "kill": "bun scripts/kill.ts",
        "monitor": "bun scripts/monitor.ts",
        "ai": "bun scripts/ai.ts"
    },
    "keywords": [
        "os",
        "kernel",
        "vfs",
        "bun",
        "typescript"
    ],
    "author": "Ian Zepp",
    "license": "SEE LICENSE IN LICENSE.md",
    "devDependencies": {
        "@eslint/js": "^9.39.1",
        "@stylistic/eslint-plugin": "^5.6.1",
        "@types/bun": "^1.3.3",
        "eslint": "^9.39.1",
        "typescript": "^5.6.3",
        "typescript-eslint": "^8.48.1"
    },
    "engines": {
        "bun": ">=1.0.0"
    },
    "dependencies": {
        "acorn-loose": "^8.5.2",
        "msgpackr": "^1.11.5"
    }
}
